apiVersion: v2
name: slack-knowledge-bot
description: Internal Slack knowledge bot — per-user ACL-enforced RAG over Notion/Confluence/Drive.
type: application
version: 0.1.0
appVersion: '0.1.0'
maintainers:
  - name: protohype
dependencies:
  # Shared named templates (serviceaccount, networkpolicy, prometheusrule helpers,
  # grafana dashboard, name/label helpers). Vendored under charts/tenant-chart-base
  # from nanohype/templates/tenant-chart-base — helm works offline, no fetch.
  - name: tenant-chart-base
    version: 0.1.0
    repository: file://charts/tenant-chart-base
