node_modules
dist
coverage
infra/cdk.out
package-lock.json
docs
packages

chart/templates
chart/dashboards

# byte-identical vendored copies (runtime modules, the sync script, the
# ESLint base) — formatted at their source of truth; local rewrites would
# fail `sync-vendored.mjs --check`
src/vendor
scripts/sync-vendored.mjs
eslint.base.mjs
